{
  "topic": "Vector Databases",
  "tier": "Tier 2 \u00b7 ~70% of postings",
  "sources": [
    {
      "name": "Qdrant docs",
      "url": "https://qdrant.tech/documentation/"
    },
    {
      "name": "Pinecone docs",
      "url": "https://docs.pinecone.io"
    },
    {
      "name": "pgvector (PostgreSQL extension)",
      "url": "https://github.com/pgvector/pgvector"
    },
    {
      "name": "Weaviate docs",
      "url": "https://weaviate.io/developers/weaviate"
    }
  ],
  "cards": [
    {
      "type": "Recall",
      "front": "What does HNSW stand for, and what problem does it solve?",
      "back": "Hierarchical Navigable Small World \u2014 an approximate nearest-neighbor search algorithm. It builds a multi-layer graph of vectors so search can skip through the graph in roughly logarithmic time instead of comparing against every vector, trading a small amount of accuracy for massive speed at scale."
    },
    {
      "type": "Why",
      "front": "Why is nearest-neighbor search \"approximate\" in production vector databases, not exact?",
      "back": "Exact nearest-neighbor search requires comparing the query against every vector in the database \u2014 fine for thousands of vectors, infeasible at millions. Approximate methods like HNSW sacrifice a small amount of recall for orders-of-magnitude speedup, which is the right tradeoff for almost all real applications.",
      "starred": true
    },
    {
      "type": "Recall",
      "front": "What's the practical tradeoff between HNSW and IVFFlat indexing?",
      "back": "HNSW: better query speed and recall, but higher memory usage and slower index build/insert time. IVFFlat (inverted file, flat storage): clusters vectors into buckets first, cheaper to build and update, but generally lower recall for the same speed \u2014 often preferred when writes are frequent or memory is constrained."
    },
    {
      "type": "Application",
      "front": "When would you choose pgvector over a dedicated vector database like Pinecone or Qdrant?",
      "back": "When your vector data needs to live alongside relational data you're already querying with SQL \u2014 joins, transactions, existing Postgres infrastructure and ops knowledge. You trade some scale ceiling and specialized performance for operational simplicity and one less system to run.",
      "starred": true
    },
    {
      "type": "Why",
      "front": "Why do managed vector DBs (Pinecone, Weaviate Cloud) vs. self-hosted (pgvector, Qdrant OSS) matter as a build-vs-buy decision?",
      "back": "Managed: no ops burden, scales automatically, but recurring cost and less control over data residency. Self-hosted: full control, often cheaper at scale, but you own uptime, scaling, and backups. This is the same build-vs-buy tradeoff that shows up everywhere in AI infra \u2014 not unique to vector stores.",
      "starred": true
    },
    {
      "type": "Recall",
      "front": "What is metadata filtering in a vector search, and why does it matter for retrieval precision?",
      "back": "Attaching structured fields (date, category, author, permission level) to each vector so a query can combine semantic similarity with SQL-like WHERE-clause filtering \u2014 e.g. \"semantically similar AND published after 2024 AND visible to this user.\" Dramatically improves precision over pure similarity search alone."
    },
    {
      "type": "Why",
      "front": "Why can hybrid search (vector + metadata filter + keyword) outperform pure vector similarity, even with a great embedding model?",
      "back": "Embeddings capture semantic meaning but can miss exact-match requirements (a specific product ID, a required date range, a permission boundary) that aren't really \"semantic\" at all. Combining retrieval modes covers both what the model understands conceptually and what the system needs to enforce structurally.",
      "starred": true
    },
    {
      "type": "Recall",
      "front": "What is vector quantization, and why would you use it?",
      "back": "Compressing vector representations (e.g. from 32-bit floats to 8-bit integers or binary) to cut memory footprint and speed up distance calculations \u2014 at the cost of some precision. Matters a lot at scale: a billion-vector index in full precision can be prohibitively expensive to hold in memory."
    },
    {
      "type": "Application",
      "front": "How do you decide the right embedding dimensionality for your use case?",
      "back": "Higher dimensions capture more nuance but cost more to store and search, and don't always improve retrieval quality proportionally. Start with what your embedding model natively outputs, benchmark retrieval quality against your own eval set, and only reduce (via a smaller model or dimensionality reduction) if storage/latency actually becomes the bottleneck \u2014 don't optimize this before you've measured it.",
      "starred": true
    },
    {
      "type": "Why",
      "front": "Why does re-indexing become a real operational concern as your corpus grows, and how do teams avoid full re-indexes?",
      "back": "Changing your embedding model means every existing vector is now in a different semantic space than new ones \u2014 you can't mix old and new vectors safely. Full re-indexing at scale is slow and costly, so production systems often version their indexes and cut over gradually, or design embedding pipelines so model upgrades are rare, deliberate events, not casual swaps.",
      "starred": true
    }
  ]
}